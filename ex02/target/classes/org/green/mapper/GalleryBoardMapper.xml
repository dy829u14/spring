<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"Http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.green.mapper.GalleryBoardMapper">
	<insert id="insert">
		insert into tbl_gallery(gno, title, writer, content, category, filename, uploadpath, fullname)
		values(seq_gboard_num.nextval, #{title}, #{writer}, #{content}, #{category}, #{filename}, #{uploadpath}, #{fullname})
	</insert>
	<select id="read" resultType="org.green.domain.GalleryBoardDTO">
		Select * from tbl_gallery where gno = #{gno}
	</select>
	<delete id="delete">
		Delete from tbl_Gallery where gno = #{gno}
	</delete>
	<update id="update">
		Update tbl_gallery set title = #{title},writer = #{writer},content = #{content},
		category =  #{category}, filename = #{filename}, uploadpath = #{uploadpath}, fullname = #{fullname} where gno = #{gno}
	</update>
	<select id="getList" resultType="org.green.domain.GalleryBoardDTO">
		Select * from tbl_gallery
	</select>
</mapper>